# YourTJ Credit Backend - 环境变量配置

# Turso 数据库配置
# 获取方式: turso db show yourtj-credit --url
TURSO_DATABASE_URL=libsql://your-database.turso.io

# Turso 访问令牌
# 获取方式: turso db tokens create yourtj-credit
TURSO_AUTH_TOKEN=your-auth-token-here

# 词库保护密钥（自己生成一个随机字符串）
# 生成方式: openssl rand -hex 32
WORDLIST_SECRET=your-secret-key-change-me-to-random-string

# 注意事项:
# 1. 本地开发时，将此文件复制为 .env 并填入真实值
# 2. 部署到 Vercel 时，在 Vercel Dashboard 的 Environment Variables 中配置
# 3. 前端的 VITE_WORDLIST_SECRET 必须与此处的 WORDLIST_SECRET 保持一致
